export class Tables {
  public static readonly USER = "users";
}

export enum UserTable {
  ID = "id",
  NAME = "name",
  EMAIL = "email",
  PASSWORD = "password",
  CREATED_AT = "createdAt",
  UPDATED_AT = "updatedAt",
}
